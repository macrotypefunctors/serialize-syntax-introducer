#lang info

;; -----------------------------------------------

;; Package Info

(define collection "serialize-syntax-introducer")

(define deps
  '("base"
    ))

(define build-deps
  '("scribble-lib"
    "racket-doc"
    ))

;; -----------------------------------------------

;; Collection Info

(define scribblings
  '(["scribblings/serialize-syntax-introducer.scrbl" ()]))

;; -----------------------------------------------

